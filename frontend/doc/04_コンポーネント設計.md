# コンポーネント設計

リアルタイム在庫・価格変動監視システムのReactコンポーネント設計思想と実装パターン

## 📋 目次

1. [設計原則](#設計原則)
2. [コンポーネント階層](#コンポーネント階層)
3. [UI コンポーネント詳細](#ui-コンポーネント詳細)
4. [データフロー設計](#データフロー設計)
5. [型安全性とプロップス設計](#型安全性とプロップス設計)
6. [再利用性とメンテナンス性](#再利用性とメンテナンス性)

---

## 設計原則

### 🎯 コアプリンシプル

1. **Single Responsibility Principle (SRP)**
   - 各コンポーネントは単一の責任を持つ
   - データ取得、表示、操作を適切に分離

2. **Container/Presentational Pattern**
   - Smart Components: データ取得とビジネスロジック
   - Dumb Components: 純粋な表示ロジック

3. **Composition over Inheritance**
   - コンポーネント合成による柔軟な設計
   - 高階コンポーネントやカスタムフックの活用

4. **Type Safety First**
   - TypeScript による厳密な型定義
   - プロップスとステートの型安全性

### 🏗️ アーキテクチャパターン

```
┌─────────────────────────────────────────────────────┐
│                 Page Components                     │
│  (pages/dashboard.tsx - Smart Components)           │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│              Feature Components                     │
│  (DashboardStats, InventoryTable - Smart)           │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│                UI Components                        │
│  (Pagination, LoadingSpinner - Dumb)                │
└─────────────────────────────────────────────────────┘
```

---

## コンポーネント階層

### 📊 階層構造

#### Level 1: Page Components
```
app/page.tsx (Dashboard Main Page)
├── PageHeader
├── DashboardStats        
├── LowStockAlert         
└── InventoryTable        
```

#### Level 2: Feature Components
```
DashboardStats
├── StatCard × 4
└── StatsOverview

InventoryTable  
├── TableHeader
├── TableBody
├── TableRow × N
└── Pagination

LowStockAlert
├── AlertHeader
├── AlertList
└── AlertItem × N
```

#### Level 3: UI Components
```
UI基盤コンポーネント
├── LoadingSpinner
├── ErrorMessage
├── ConnectionStatus
├── PageHeader
└── Pagination
```

---

## UI コンポーネント詳細

### 📋 DashboardStats コンポーネント

#### 実装概要
```typescript
interface DashboardStatsProps {
  inventoryData?: PaginatedResponse<InventoryItem>;
  lowStockItems?: InventoryItem[];
  statsData?: InventoryStats;
  className?: string;
}

export function DashboardStats({
  inventoryData,
  lowStockItems,
  statsData,
  className = '',
}: DashboardStatsProps) {
  // 統計データの計算とフォールバック処理
  // StatCardコンポーネントの組み立て
  // レスポンシブレイアウトの実装
}
```

#### 設計特徴
- **データフォールバック**: 複数のデータソースから統計を計算
- **レスポンシブ**: Grid レイアウトで画面サイズに対応
- **カラーシステム**: ステータス別の一貫した色使い
- **アイコン統合**: Lucide React でビジュアル強化

#### StatCard サブコンポーネント
```typescript
interface StatCardProps {
  title: string;
  value: string | number;
  icon: React.ReactNode;
  description?: string;
  color: 'blue' | 'green' | 'yellow' | 'red';
  className?: string;
}
```

---

### 📊 InventoryTable コンポーネント

#### 実装概要
```typescript
interface InventoryTableProps {
  data?: PaginatedResponse<InventoryItem>;
  onRefresh: () => void;
  onPageChange: (page: number) => void;
  onItemsPerPageChange: (itemsPerPage: number) => void;
  className?: string;
}

export function InventoryTable({
  data,
  onRefresh,
  onPageChange,
  onItemsPerPageChange,
  className = '',
}: InventoryTableProps) {
  // テーブル表示ロジック
  // ソート機能（将来拡張）
  // ページネーション統合
}
```

#### 設計特徴
- **ページネーション統合**: 自動的な件数管理
- **ソート可能**: ヘッダークリックでソート（拡張可能）
- **レスポンシブ**: モバイル対応のテーブル表示
- **アクセシビリティ**: スクリーンリーダー対応
- **ローディング状態**: Skeleton UI で UX 向上

#### テーブル行の動的スタイリング
```typescript
const getRowClassName = (item: InventoryItem) => {
  if (item.stock_quantity <= 0) {
    return 'bg-red-50 border-red-200'; // 在庫切れ
  }
  if (item.is_low_stock) {
    return 'bg-yellow-50 border-yellow-200'; // 低在庫
  }
  return 'bg-white hover:bg-gray-50'; // 通常
};
```

---

### 🚨 LowStockAlert コンポーネント

#### 実装概要
```typescript
interface LowStockAlertProps {
  items?: InventoryItem[];
  onRefresh: () => void;
  className?: string;
}

export function LowStockAlert({
  items,
  onRefresh,
  className = '',
}: LowStockAlertProps) {
  // 低在庫アイテムの表示
  // 在庫レベル可視化
  // アクション可能なUI
}
```

#### 設計特徴
- **視覚的警告**: 色とアイコンで緊急度を表現
- **在庫レベル表示**: プログレスバーで視覚化
- **アクション統合**: 更新ボタンで即座に最新情報取得
- **条件付き表示**: アイテムがある場合のみ表示

#### StockLevelBar サブコンポーネント
```typescript
interface StockLevelBarProps {
  current: number;
  minimum: number;
  maximum: number;
  className?: string;
}

const StockLevelBar: React.FC<StockLevelBarProps> = ({
  current,
  minimum,
  maximum,
}) => {
  const percentage = Math.min((current / maximum) * 100, 100);
  const isLow = current <= minimum;
  
  return (
    <div className="w-full bg-gray-200 rounded-full h-2">
      <div
        className={`h-2 rounded-full transition-all duration-300 ${
          isLow ? 'bg-red-500' : 'bg-green-500'
        }`}
        style={{ width: `${percentage}%` }}
      />
    </div>
  );
};
```

---

### 🎨 UI共通コンポーネント

#### Pagination コンポーネント
```typescript
interface PaginationProps {
  currentPage: number;
  totalPages: number;
  totalItems: number;
  itemsPerPage: number;
  onPageChange: (page: number) => void;
  onItemsPerPageChange: (itemsPerPage: number) => void;
  className?: string;
}
```

**特徴**:
- 完全な型安全性
- アクセシビリティ対応
- カスタマイズ可能なスタイリング
- 境界値チェック

#### LoadingSpinner コンポーネント
```typescript
interface LoadingSpinnerProps {
  size?: 'sm' | 'md' | 'lg';
  className?: string;
}

export const LoadingSpinner: React.FC<LoadingSpinnerProps> = ({
  size = 'md',
  className = '',
}) => {
  const sizeClasses = {
    sm: 'h-4 w-4',
    md: 'h-8 w-8', 
    lg: 'h-12 w-12',
  };

  return (
    <div className={`${sizeClasses[size]} ${className}`}>
      <div className="animate-spin rounded-full border-2 border-gray-300 border-t-blue-600" />
    </div>
  );
};
```

#### ErrorMessage コンポーネント
```typescript
interface ErrorMessageProps {
  title?: string;
  message: string;
  onRetry?: () => void;
  className?: string;
}
```

**エラー表示戦略**:
- 階層的エラー情報（タイトル + 詳細）
- 再試行アクション統合
- 一貫したビジュアルデザイン
- アクセシビリティ考慮

---

## データフロー設計

### 🔄 データの流れ

#### 1. トップダウンデータフロー
```typescript
// Page Level
function Dashboard() {
  // データ取得フック
  const { data: inventoryData, refetch: refetchInventory } = useInventoryList();
  const { data: lowStockItems } = useLowStockItems();
  const { data: statsData } = useInventoryStats();

  return (
    <>
      <DashboardStats 
        inventoryData={inventoryData}
        lowStockItems={lowStockItems}
        statsData={statsData}
      />
      <InventoryTable 
        data={inventoryData}
        onRefresh={refetchInventory}
        onPageChange={handlePageChange}
      />
    </>
  );
}
```

#### 2. イベントドリブンフロー
```typescript
// コンポーネント間のイベント伝播
const handlePageChange = (page: number) => {
  // ページネーション状態更新
  setCurrentPage(page);
  // データ再取得（TanStack Queryが自動実行）
};

const handleRefresh = () => {
  // 手動更新要求
  refetchInventory();
  // 関連データも連鎖更新
};
```

#### 3. リアルタイムデータフロー
```typescript
// WebSocket → TanStack Query → React Components
useEffect(() => {
  const { handleInventoryUpdate } = useInventoryRealTimeUpdates();
  
  // WebSocketイベントリスナー
  websocket.addEventListener('message', (event) => {
    const data = JSON.parse(event.data);
    handleInventoryUpdate(data); // キャッシュ更新
    // → 自動的にコンポーネント再レンダリング
  });
}, []);
```

---

## 型安全性とプロップス設計

### 🛡️ TypeScript統合

#### 厳密な型定義
```typescript
// API型と完全同期
export interface InventoryItem {
  id: number;
  name: string;
  sku: string;
  stock_quantity: number;
  min_stock_level: number;
  // ... 他のフィールド
  
  // 計算プロパティも型定義
  available_quantity: number;
  is_low_stock: boolean;
  stock_status: string;
}

// ページネーション型
export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  per_page: number;
  pages: number;
}
```

#### コンポーネントプロップス設計
```typescript
// 基本原則: 必須 vs オプショナル の明確な区分
interface ComponentProps {
  // 必須プロップス: コンポーネント動作に不可欠
  data: PaginatedResponse<InventoryItem>;
  onPageChange: (page: number) => void;
  
  // オプショナルプロップス: デフォルト値あり
  className?: string;
  isLoading?: boolean;
  error?: Error | null;
}
```

#### 条件付き型の活用
```typescript
// プロップスの組み合わせを型で制御
type AlertProps = 
  | { type: 'info'; message: string }
  | { type: 'error'; error: Error; onRetry: () => void }
  | { type: 'loading'; progress?: number };
```

### 🔒 型安全なイベント処理

```typescript
// イベントハンドラーの型安全性
interface TableEvents {
  onSort: (field: keyof InventoryItem, direction: 'asc' | 'desc') => void;
  onSelect: (itemId: number, selected: boolean) => void;
  onAction: (action: 'edit' | 'delete' | 'duplicate', itemId: number) => void;
}
```

---

## 再利用性とメンテナンス性

### 🔧 再利用可能な設計

#### 1. 汎用UIコンポーネント
```typescript
// 汎用テーブルコンポーネント
interface DataTableProps<T> {
  data: T[];
  columns: ColumnDefinition<T>[];
  loading?: boolean;
  error?: Error | null;
  pagination?: PaginationProps;
  className?: string;
}

export function DataTable<T extends Record<string, any>>({
  data,
  columns,
  loading,
  error,
  pagination,
  className,
}: DataTableProps<T>) {
  // 汎用テーブル実装
}
```

#### 2. カスタムフックの抽象化
```typescript
// 汎用データ取得フック
export function usePagedData<T>(
  fetcher: (skip: number, limit: number) => Promise<PaginatedResponse<T>>,
  options?: UseQueryOptions
) {
  const [pagination, setPagination] = usePagination();
  
  const query = useQuery({
    queryKey: ['paged-data', pagination.skip, pagination.limit],
    queryFn: () => fetcher(pagination.skip, pagination.limit),
    ...options,
  });
  
  return {
    ...query,
    pagination,
    handlePageChange: setPagination.setPage,
    handleItemsPerPageChange: setPagination.setItemsPerPage,
  };
}
```

### 🎨 スタイリング戦略

#### 1. Tailwind CSS設計原則
```typescript
// 一貫したデザイントークン
const designTokens = {
  colors: {
    primary: 'blue',
    success: 'green', 
    warning: 'yellow',
    danger: 'red',
  },
  spacing: {
    card: 'p-6',
    section: 'mb-8',
    element: 'mb-4',
  },
  shadows: {
    card: 'shadow-sm',
    elevated: 'shadow-md',
  },
};
```

#### 2. コンポーネント別スタイリング
```typescript
// スタイル関数での一貫性確保
const getCardStyle = (variant: 'default' | 'warning' | 'error') => {
  const base = 'rounded-lg border p-6 bg-white';
  const variants = {
    default: 'border-gray-200',
    warning: 'border-yellow-200 bg-yellow-50',
    error: 'border-red-200 bg-red-50',
  };
  
  return `${base} ${variants[variant]}`;
};
```

### 📱 レスポンシブ設計

#### モバイルファースト設計
```typescript
const ResponsiveTable: React.FC<TableProps> = ({ data, columns }) => {
  return (
    <>
      {/* デスクトップ: フルテーブル */}
      <div className="hidden md:block">
        <table className="min-w-full">
          {/* テーブル実装 */}
        </table>
      </div>
      
      {/* モバイル: カード形式 */}
      <div className="md:hidden space-y-4">
        {data.map(item => (
          <div key={item.id} className="bg-white p-4 rounded-lg shadow">
            {/* カード形式の実装 */}
          </div>
        ))}
      </div>
    </>
  );
};
```

---

## 🧪 テストとドキュメント

### テスタブルな設計

#### 1. 純粋関数コンポーネント
```typescript
// テストしやすい純粋関数
export const formatStockLevel = (
  current: number, 
  minimum: number
): { level: 'high' | 'low' | 'out', percentage: number } => {
  if (current <= 0) return { level: 'out', percentage: 0 };
  if (current <= minimum) return { level: 'low', percentage: (current / minimum) * 100 };
  return { level: 'high', percentage: 100 };
};
```

#### 2. プロップス型エクスポート
```typescript
// テストでの型再利用
export type { DashboardStatsProps, InventoryTableProps };
```

### コンポーネントドキュメント
```typescript
/**
 * 在庫統計ダッシュボード
 * 
 * @description 在庫データの概要統計を4つのカードで表示
 * @param inventoryData - ページネーション済み在庫データ
 * @param lowStockItems - 低在庫アイテム配列
 * @param statsData - 統計API からのデータ
 * 
 * @example
 * <DashboardStats 
 *   inventoryData={inventoryData}
 *   lowStockItems={lowStockItems}
 *   statsData={statsData}
 * />
 */
```

---

この設計により、保守性、拡張性、テスタビリティを兼ね備えた堅牢なフロントエンドアーキテクチャを実現しています。